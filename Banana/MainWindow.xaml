<Window x:Class="Banana.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Banana"
        xmlns:vm="clr-namespace:Banana.ViewModels"
        xmlns:v="clr-namespace:Banana.Views"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        
        Style="{StaticResource MaterialDesignWindow}"
        
        DataContext="{Binding  MainWindowViewModel, Source={StaticResource ServiceLocator}}"
        >
        <!--        Style="{StaticResource MaterialDesignWindow}" Content="{Binding Content}"-->
        
        <Grid>
                <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"></ColumnDefinition>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                
                
                <ContentControl Grid.Column="0" Content="{Binding Content}"></ContentControl>
                <ContentControl Grid.Column="1" Content="{Binding Content}"></ContentControl>
        </Grid>

    
</Window>
